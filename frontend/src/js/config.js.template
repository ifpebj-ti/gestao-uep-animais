/* Gerado automaticamente na inicialização do container a partir das
   variáveis de ambiente API_URL e GOOGLE_CLIENT_ID (ver frontend/.env.example
   e docker-entrypoint.d/40-generate-api-config.sh). NÃO edite o arquivo
   js/config.js gerado — edite este template ou as variáveis de ambiente. */
window.__APP_CONFIG__ = {
  API_BASE: "${API_URL}/api",
  GOOGLE_CLIENT_ID: "${GOOGLE_CLIENT_ID}"
};
